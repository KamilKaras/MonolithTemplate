import AppStateCard from "../../molecules/AppStateCard/AppStateCard";

const HomePage = () => {
  return (
    <AppStateCard
      title="Starter Home"
      message="Authenticated starter area is ready for feature implementation."
    />
  );
};

export default HomePage;
